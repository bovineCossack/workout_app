import React from 'react';
import Button from './Button';

export const Default = () => (
  <Button
    handleClick={() => {
      //
    }}
  >
    My Button
  </Button>
);

export default {
  title: 'Button',
  component: Button,
};
